import 'package:flutter/material.dart';

class PreviewSongs extends StatelessWidget {
  const PreviewSongs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
